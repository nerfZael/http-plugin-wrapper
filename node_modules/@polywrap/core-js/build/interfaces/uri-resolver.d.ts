import { Uri, Invoker, InvokeResult } from "../";
export interface MaybeUriOrManifest {
    uri?: string;
    manifest?: Uint8Array;
}
export declare const module: {
    tryResolveUri: (invoker: Invoker, wrapper: Uri, uri: Uri) => Promise<InvokeResult<MaybeUriOrManifest>>;
    getFile: (invoker: Invoker, wrapper: Uri, path: string) => Promise<InvokeResult<Uint8Array>>;
};
