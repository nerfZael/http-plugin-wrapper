import { AbiTransforms } from ".";
import { PropertyDefinition, Abi } from "../abi";
export declare const finalizePropertyDef: (abi: Abi) => AbiTransforms;
export declare function populatePropertyType(property: PropertyDefinition, abi: Abi): void;
